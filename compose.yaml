services:
    db:
        container_name: db
        image: bendereg/postgres_memo
        env_file:
           - .env
        volumes:
            - memo_db:/var/lib/postgresql/data
        networks:
            - app
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
            interval: 5s
            timeout: 5s
            retries: 5

    red:
        container_name: red
        image: redis
        volumes:
            - test_red:/data
        networks:
            - app
        healthcheck:
            test: ["CMD-SHELL", "redis-cli ping | grep PONG"]
            interval: 1s
            timeout: 3s
            retries: 5

    memo_main:
        container_name: memo_main
        image: memo_main
        depends_on:
            - db
            - red
        env_file:
            - .env
        networks:
            - app
        restart: always

volumes:
    memo_db:
        name: memo_db
    test_red:
        name: test_red

networks:
    app:
        name: app
